# Force public npm registry. Prevents a developer's global ~/.npmrc
# (which may point at an internal artifact mirror) from leaking proprietary
# URLs into package-lock.json.
registry=https://registry.npmjs.org/
